solon.logging.appender:
  console:
    enable: false
  file:
    level: DEBUG

solon.code.cli:
  name: "小花"
  #  instruction: "你是一个超级智能体（什么问题都能解决），办事高效简洁，性格幽默风趣。" # 引导指令
  workDir: "work"
  maxSteps: 100
  maxStepsAutoExtensible: true
  sessionWindowSize: 10

  planningMode: true
  hitlEnabled: false

  cliEnabled: true
  cliPrintSimplified: true

  webEnabled: false
  webEndpoint: "/cli"

  acpEnabled: false
  acpTransport: "websocket" # "stdio" or "websocket"
  acpEndpoint: "/acp"

  chatModel:
    apiUrl: "https://apis.iflow.cn/v1/chat/completions"
    apiKey: "sk-18e1e33e634969e5a3e13a01ebd2a907"
    model: "glm-4.6"
  mountPool:
    "@shared": "${user.home}/WORK/work_github/solonlab/awesome-claude-skills"
